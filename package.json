{
  "name": "punjab-ready-portal",
  "version": "1.0.0",
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd dashboard && npm run dev",
    "build:frontend": "cd dashboard && npm run build",
    "install:all": "npm install && cd backend && npm install && cd ../dashboard && npm install"
  },
  "devDependencies": {
    "vercel": "^35.0.0"
  }
}